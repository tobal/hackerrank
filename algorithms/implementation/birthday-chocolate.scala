object Solution {

    def main(args: Array[String]) {
        val sc = new java.util.Scanner (System.in);
        var n = sc.nextInt();
        var s = new Array[Int](n);
        for(s_i <- 0 to n-1) {
           s(s_i) = sc.nextInt();
        }
        var d = sc.nextInt();
        var m = sc.nextInt();
    }
}
